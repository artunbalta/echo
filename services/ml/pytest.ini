[pytest]
testpaths = tests
# Apple Accelerate + NumPy 2.0 emits spurious matmul RuntimeWarnings on macOS; the
# numerical results are correct (asserted by the tests). Silence only that noise.
filterwarnings =
    ignore:.*encountered in matmul.*:RuntimeWarning
