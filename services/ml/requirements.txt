fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
numpy==2.0.2
httpx==0.28.1
python-dotenv==1.0.1
pytest==8.3.4
# Optional: real multilingual embeddings via Voyage AI. NOT required — embeddings.py falls
# back to the REST API and then to the deterministic hash mock. Install to enable the SDK:
#   pip install voyageai
